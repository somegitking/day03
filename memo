변수 ->제어문 ->반복문 ->배열

직접: 변수 직접적인 값
간접: 참조/레퍼런스/리모콘

const arr = []

복습시
1. 문법 복습
2. 프로그래밍 능력/방법

팀을 계속 바꿈
7층 휴게실

5월 취업하기 좋음
4월 말에 준비가 끝나야 함

모의면접
모의면접 많이 볼수록 취업률 높음

js
모든 것은 객체다
메모리를 차지하면 객체

자바 : 인스턴스
배열도 객체

자바
int[] arr = new int[]; //에러
int[] arr = new int[5]; //노에러

js
let arr = []; //노에러
링크드 리스트 구조
기차 연결 구조
중요특징 : 키값으로 되어 있음, 책 뒷페이지에 인덱스와 비슷 /

js 배열 특징
규칙 x 하나의미 x
반복문

for 루프 ->카운팅 루프

면접 팁
시험 보는 회사
회사의 직원 채용에 있어 협상력을 강화, 높은 포지션에서 협상하기 위해서
약점을 잡히지 말것

for(1(초기 실행), 2(조건, 생략 가능), 4(마지막 실행문 ){
    3//실행문
} //실행 순서

for( ;  ; ) == while(true)

면접 문제
for -> while로 바꾸는 문제

do-while //한번의 실행을 보증하고 싶을 때
do{

}while( i <20) {

}

let flag = false
if(!flag


null : 껍데기 리모콘

자바에서 변수가

//리모컨이 들어갈 수 있는 것은 대문자, 배열

String str = null; //앞글자가 대문자면 null 들어갈 수 있음

nullpointerexcepttion은 .이나 꺽쇠 에서만 발생

디버그로 돌리면

데이터 안에 데이터가 들어가는 데이터

다차원 배열

다차원 배열 학습 이유

const arr = [[1, 2] [3, 4]]
arr -> 리모콘 1 -> [1,2]
     ->리모콘2 ->[3,4]

자바에서
arr[0] : 주소값 ->arr[0][0] -> 값
arr[1] : 주소값 ->arr[1][0] -> 값

자바에서 nullpointexception 발생하면 그 라인에서 .이나 꺽쇠에서 만 발생

달려라 코드학원

어려우면 수업에 포커스
이해할수 있으면 코딩/프로그래밍에 포커스

휴게실에서 수업들을 수 있음.